# pollpoll-android
3조 pollpoll android 앱입니다.

### Dependency Diagram

```mermaid
%%{
  init: {
    'theme': 'neutral'
  }
}%%

graph LR

  app --> core-testing
  app --> core-ui
  app --> core-data
  app --> feature-login
  app --> feature-vote
  app --> feature-mypollpoll
  app --> feature-settings
  app --> core-network
  app --> core-designsystem
  app --> core-common
  app --> core-model
  app --> core-ui
  app --> core-data
  app --> feature-login
  app --> feature-vote
  app --> feature-mypollpoll
  app --> feature-settings
  app --> core-network
  app --> core-designsystem
  app --> core-common
  app --> core-model
  app --> core-ui
  app --> core-data
  app --> feature-login
  app --> feature-vote
  app --> feature-mypollpoll
  app --> feature-settings
  app --> core-network
  app --> core-designsystem
  app --> core-common
  app --> core-model
  app --> core-ui
  app --> core-data
  app --> feature-login
  app --> feature-vote
  app --> feature-mypollpoll
  app --> feature-settings
  app --> core-network
  app --> core-designsystem
  app --> core-common
  app --> core-model

```